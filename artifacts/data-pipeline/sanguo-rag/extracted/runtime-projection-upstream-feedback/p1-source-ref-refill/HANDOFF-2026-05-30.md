# P1 SourceRef Refill Handoff

## 目的
- 這一輪不是下游 projection patch，而是上游 ETL / evidence refill。
- 目標是先處理 manifest 高頻 `sourceRef`：`011#p14`、`014#p10`、`042#p1`，並優先回收 `guan-yu -> cao-cao`、`le-jin -> cao-cao`、`xu-zhu -> cao-cao` 這三組 pair。

## 本輪新增 / 修改
- `local/3klife-npc-brain/pipelines/sanguo-rag/materialize_runtime_projection_refill_overlays.py`
  - 新增最小 materializer，把 overlay spec 套到 base `source-event-packets` / `relationship-evidence`，輸出 staged JSONL。
- `artifacts/data-pipeline/sanguo-rag/extracted/runtime-projection-upstream-feedback/p1-source-ref-overlay-spec.json`
  - 宣告這輪補的 packet patches 與 relationship evidence adds。
- `artifacts/data-pipeline/sanguo-rag/extracted/runtime-projection-upstream-feedback/p1-source-ref-refill/manifest-base-events/runtime-projection-upstream-refill-manifest.json`
- `artifacts/data-pipeline/sanguo-rag/extracted/runtime-projection-upstream-feedback/p1-source-ref-refill/manifest-base-events/runtime-projection-upstream-refill-summary.md`
  - 保留這輪 benchmark 後的 manifest summary。

## 本輪實際補了什麼
- packet patches: 3 個 `sourceRef`
  - `011#p14`: 補 `le-jin -> cao-cao`
  - `014#p10`: 補 `le-jin -> cao-cao`、`xu-zhu -> cao-cao`
  - `042#p1`: 補 `zhang-fei -> cao-cao` packet edge 對齊現有 ready-event
- relationship evidence adds: 6 筆
  - `011#p14`: `le-jin -> cao-cao`
  - `014#p10`: `le-jin -> cao-cao`、`xu-zhu -> cao-cao`
  - `042#p1`: `guan-yu -> cao-cao`、`le-jin -> cao-cao`、`xu-zhu -> cao-cao`

## 關鍵判斷
- `sourceHighlight` / queue 是否下降，真正關鍵不是 packet 自帶 `relationshipEdges`，而是 `relationship-evidence.evidenceRefs` 是否命中該 `sourceRef`。
- packet `relationshipEdges` 仍值得補，因為它是 upstream audit / promotion 的一部分，但如果只補 packet edges、不補 relationship refs，queue 不會直接掉。
- 這輪正式驗證採用「base ready-events + staged relationship-evidence」benchmark；不把 packet promotion 納入正式數字，因為 promotion 會額外打開新的 projection surface，干擾 queue 總量比較。

## 已執行流程
1. 用 overlay spec materialize staged inputs
2. 用 `base ready-events + staged relationship-evidence` 重跑 Top50 runtime export
3. 重建 projection feedback queue
4. 重建 refill manifest

## 驗證結果
- targeted pair/sourceRef rows 已清零
  - baseline 共有 10 rows
  - benchmark queue 變成 0 rows
- queue proposal 總量
  - baseline: `9277`
  - current: `9264`
  - net: `-13`
- top sourceRef counts
  - `014#p10`: `408 -> 404`
  - `011#p14`: `221 -> 218`
  - `042#p1`: `218 -> 212`
- pair-level P1 occurrence
  - `guan-yu -> cao-cao`: `19 -> 18`
  - `le-jin -> cao-cao`: `19 -> 14`
  - `xu-zhu -> cao-cao`: `19 -> 16`
- `P1` bucket 總量仍是 `2700`
  - 代表這輪已經回收 targeted occurrences，但還沒把整個 bucket 大幅壓下去

## 重要輸出路徑
- overlay spec
  - `artifacts/data-pipeline/sanguo-rag/extracted/runtime-projection-upstream-feedback/p1-source-ref-overlay-spec.json`
- staged refill root
  - `artifacts/data-pipeline/sanguo-rag/extracted/runtime-projection-upstream-feedback/p1-source-ref-refill/`
- base-events benchmark queue summary
  - `artifacts/data-pipeline/sanguo-rag/extracted/runtime-projection-upstream-feedback/p1-source-ref-refill/queue-base-events/runtime-projection-upstream-feedback-summary.json`
- base-events benchmark manifest summary
  - `artifacts/data-pipeline/sanguo-rag/extracted/runtime-projection-upstream-feedback/p1-source-ref-refill/manifest-base-events/runtime-projection-upstream-refill-summary.md`

## blocker / 風險
- `042#p1` 仍然有 alias-only blocker sourceRef 殘量，這輪只解掉指定 pair/sourceRef gap，沒有把整個長坂橋 cluster 全面收乾淨。
- `P1` bucket 沒有整體下降，代表後續還要沿同一群 Cao-Cao / Wei 側 relationship-authority backfill 繼續補。
- `local/` 與 `artifacts/data-pipeline/` 在 repo 預設是 ignored；若要提交這些檔案，需要明確 `git add -f`。

## 下一位 AI 建議接手順序
1. 先延續 `le-jin -> cao-cao` / `xu-zhu -> cao-cao` 同群組剩餘高頻 `sourceRef`
   - 優先看 `011#p15`、`014#p6`、`016#p17`、`031#p8`
2. 再處理 `cao-ren -> cao-cao`、`li-dian -> cao-cao`、`xiahou-dun -> cao-cao`
   - 這些現在已經浮到 P1 前排
3. 對 `042#p1` 不要只補 `battlefield_contact`
   - 若下一輪要壓更多 occurrence，先回讀 ready-event 與更多 source sentence，確認是否可升成更明確的 authority / scene linkage
4. 每補一輪，都沿用這個閉環
   - materialize overlays
   - export runtime profiles
   - build feedback queue
   - build refill manifest
   - 只用同一個 benchmark 口徑比較

## 可直接對下一位 AI 說的話
- 你現在是上游 ETL / 補證據 AI，不是下游 projection patch AI。
- 先接 `p1-source-ref-overlay-spec.json` 與 `p1-source-ref-refill/HANDOFF-2026-05-30.md`。
- 你的目標不是把 queue 清零，而是沿同一個 pair cluster 持續降低 `relationship-authority-backfill` 的 occurrence，優先打 Cao-Cao 相關高頻 group。
- 除非有新的 source-grounded sentence，否則不要把 alias-only gap 硬升成 scene authority。
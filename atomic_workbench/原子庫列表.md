# 原子庫列表

> 來源：`AI-Atomic-Framework/atomic-registry.json`
>
> `類別推導` 是給人類看的整理欄位，不是 registry schema 的正式欄位。
>
> 版本待辦：目前 root `package.json` 仍是 `0.0.0`；若這批 alpha0 pass 已確認，下一步建議 bump 為 `0.1.0-alpha.0`，並補上一個對應的 git tag。

## 目前 registry 清單

| id | 功能 | 類別推導 | 狀態 | spec 路徑 |
|---|---|---|---|---|
| `atom.core-seed` | ATM Core Seed Self Descriptor：將 `ATM-CORE-0001` 對應到目前 atomic-spec vocabulary 的核心種子自我描述規格。 | 基礎核心 / seed / self-descriptor | `governed` | `../AI-Atomic-Framework/specs/atom-seed-spec.json` |
| `atom.plugin-rule-guard.neutrality-scanner` | 中立性掃描器：檢查受保護的 framework 表面是否出現宿主專屬詞彙或路徑，違規即阻擋。 | 治理規則 / plugin-rule-guard / scanner | `governed` | `../AI-Atomic-Framework/specs/neutrality-scanner.atom.json` |

## 怎麼知道目前清單

- 以 `AI-Atomic-Framework/atomic-registry.json` 當目前 registry 的來源。
- 想看正式欄位與狀態定義，對照 `AI-Atomic-Framework/schemas/registry.schema.json`。
- 想看單一 atom 的功能說明，對照對應的 `AI-Atomic-Framework/specs/*.atom.json` 裡的 `title` 與 `description`。

## 補充

- 目前這份清單只列出 registry 裡已登記的 governed entry。
- 若後續新增 atom，請先更新 `atomic-registry.json`，再回寫這份表。
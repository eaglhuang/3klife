---
doc_id: doc_task_0294
id: ATM-2-0010
priority: P0
phase: ATM-2
created: 2026-05-05
created_by_agent: GitHubCopilot
owner: GitHubCopilot
status: open
type: open-source-core
depends:
  - ATM-2-0007
  - ATM-2-0008
notes: "2026-05-05 | 狀態: open | 驗證: pending | 變更: 補 neutrality/boundary rule guard 任務 | 阻塞: none"
---
# [ATM-2-0010] Neutrality boundary rule guard

> **ATM self-hosting first 開卡** — 建立可自動驗證的 neutrality/boundary guard，避免 upstream 再被 adopter 私有假設污染。
> **定位**：ATM-2
> **前置依賴**：ATM-2-0007、ATM-2-0008

## 問題描述

建立 upstream neutral/boundary rule guard，先以 deterministic 掃描檢查寫死 host 假設，再以 optional semantic audit 補抓隱性耦合與 stale docs。

## INPUT_CONTRACT

- governance schema 與 local reference plugins 已定義
- guard 需同時涵蓋 code 與 docs surfaces

## OUTPUT_CONTRACT

- [ ] deterministic neutrality scan
- [ ] optional semantic audit
- [ ] protected surface policy 與 CI smoke
- [ ] scanner 需定義 allow-path for `docs/case-studies/*` 與 `examples/legacy-strangler-*` 內容（可提及 adopter domain 做反例/教學，但 core/schema/template 仍 hard block）

- [ ] deterministic / semantic 掃描必須同時涵蓋 `.atm/reports/upgrade-proposals/*` 與 evidence-store payload。
- [ ] adopter 私有資訊（3KLife / Cocos / UCUF / html-to-ucuf）出現在 upgrade-proposal 或 evidence 內容時 hard fail。

## 交付物

- [ ] neutrality boundary rule guard 規格與 deterministic scanner 實作。
- [ ] protected surface / allow-path policy，明確區分 upstream core 與 `docs/case-studies/*`、`examples/legacy-strangler-*` 教學內容。
- [ ] semantic audit report schema，覆蓋 `.atm/reports/upgrade-proposals/*` 與 evidence-store payload。
- [ ] banned adopter-private term fixtures 與 hard-fail smoke。
- [ ] CI smoke / validation wiring。

## VALIDATION_CMD

```bash
npm.cmd run check:encoding:touched -- --files docs/agent-briefs/tasks/ATM-2-0010.md
npm test
npm run typecheck
npm run lint
```

## ROLLBACK_HINT

```bash
回退 guard 規格或 reference implementation；不得把 3KLife 私有路徑寫回 upstream protected surfaces。
```

## 執行步驟

1. 定義 protected surfaces
2. 定義 banned terms 與 deterministic rules
3. 補 semantic review report schema
4. 建立 smoke fixture

*由 GitHubCopilot 透過 task-card-opener 開立 | 2026-05-05*

---
doc_id: doc_task_0318
id: ATM-2-0015
priority: P0
phase: ATM-2
created: 2026-05-06
created_by_agent: GitHubCopilot
owner: GitHubCopilot
status: open
type: registry
chain_id: ATM-EVOLUTION-V2
chain_step: 2/11
sensor_triggered_by: atm-evolution-plan-v2
depends:
  - ATM-2-0014
notes: |
  2026-05-06 | 狀態: open | 驗證: pending | 變更: 由 ATM 演進規劃書 v2 附錄 A 開立 | 阻塞: 等依賴卡完成
  2026-05-07 識別/行為/狀態機提案：追加 acceptance（schema-additive，不重啟驗收）— hash-diff-report 追加 `semanticFingerprintDelta` 與 `lineageContinuity` 旗標
---
# [ATM-2-0015] Hash Drift / Version Diff Report

> **ATM 演進規劃 v2 開卡** — 提供 atom 版本間 hash 差異報告，作為 upgrade proposal 與 rollback proof 的基礎輸入。
> **定位**：ATM-2
> **前置依賴**：ATM-2-0014

## 問題描述

產出機器可讀的 hash-diff-report，比對任兩個 atom version 的 spec/code/test hash 差異與 drift reason。

## INPUT_CONTRACT

- 依賴卡片已 done 或可作 stub
- ATM 演進規劃書 v2 附錄 A 已併入規劃

## OUTPUT_CONTRACT (acceptance)

1. hash-diff-report.schema.json 定義 fromVersion / toVersion / specHashDelta / codeHashDelta / testHashDelta / driftReason
2. CLI atm registry diff <atomId> --from <v1> --to <v2> --json 輸出符合 schema 的報告
3. fixtures：identical hash（empty delta）/ all-three-changed / driftReason missing 三種

### 2026-05-07 識別/行為提案追加（不重啟驗收）

4. hash-diff-report 追加 `semanticFingerprintDelta`（atom 行為指紋變化標記）與 `lineageContinuity` 旗標（lineage chain 是否連續）

## 交付物

- schemas/registry/hash-diff-report.schema.json
- packages/core/src/registry/diff.ts
- packages/cli/commands/registry-diff.ts

## VALIDATION_CMD

```bash
npm test; npm run typecheck; npm run lint
```

## ROLLBACK_HINT

```bash
回退本卡 touched files；保持 schema-additive 與 adapter / core 邊界。
```

## 執行步驟

1. 在上游 repo 建立或修改對應 package / schema
2. 補 fixtures（含 positive / negative 與舊版相容）
3. 跑 package 驗證
4. 回寫 3KLife tracking notes 與 thin index

*由 GitHubCopilot 透過 ATM 演進規劃書 v2 開立 | 2026-05-06*

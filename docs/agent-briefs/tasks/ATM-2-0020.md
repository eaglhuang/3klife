---
doc_id: doc_task_0323
id: ATM-2-0020
priority: P0
phase: ATM-2
created: 2026-05-06
created_by_agent: GitHubCopilot
owner: GitHubCopilot
status: open
type: orchestration-atom
chain_id: ATM-EVOLUTION-V2
chain_step: 7/11
sensor_triggered_by: atm-evolution-plan-v2
depends:
  - ATM-2-0015
  - ATM-2-0016
  - ATM-2-0017
notes: |
  2026-05-06 | 狀態: open | 驗證: pending | 變更: 由 ATM 演進規劃書 v2 附錄 A 開立 | 阻塞: 等依賴卡完成
  2026-05-07 識別/行為/狀態機提案：追加 acceptance（schema-additive，不重啟驗收）— upgrade-proposal 追加 `behaviorId` 欄位（必填，預設 `behavior.evolve`）
  2026-05-07 附錄 C 治理補丁：升版 proposal 保持為唯一編排 choke point；effect node 僅能寫 evidence，不得直接 promote
---
# [ATM-2-0020] ProposeAtomicUpgrade（000012）

> **ATM 演進規劃 v2 開卡** — evolution 管線首個 orchestration atom：把 birth-pipeline 工具組合成 upgrade 提案。
> **定位**：ATM-2
> **前置依賴**：ATM-2-0015 / ATM-2-0016 / ATM-2-0017

## 問題描述

串接 hash-diff / metrics / regression-compare 結果，產出 upgrade-proposal.json，並提供 atm upgrade --propose --dry-run CLI。

## INPUT_CONTRACT

- 依賴卡片已 done 或可作 stub
- ATM 演進規劃書 v2 附錄 A 已併入規劃

## OUTPUT_CONTRACT (acceptance)

1. upgrade-proposal.schema.json 含 atomId / fromVersion / toVersion / automatedGates / humanReview pending
2. CLI atm upgrade --propose --atom <id> --to <ver> --dry-run --json 輸出符合 schema 的 proposal
3. automatedGates 任一 fail 時 proposal 必須標記 status=blocked，不允許進入 review queue

### 2026-05-06 附錄 B 原子地圖補丁（追加 acceptance）

4. upgrade-proposal.schema.json 增加 `decompositionDecision`（enum: `atom-bump | atom-extract | map-bump`）與 `mapImpactScope` 欄位
5. CLI 增加 `--target atom|map` 旗標；`--target map` 時必須附 `mapId`
6. `decompositionDecision` 為 `atom-extract` 時，proposal 需同時列出新 atomId 與 fork 來源

### 2026-05-07 識別/行為提案追加（不重啟驗收）

7. upgrade-proposal.schema.json 追加必填欄位 `behaviorId`（預設 `behavior.evolve`）；其他合法值由 ATM-2-0029 reference behavior pack 註冊（split / merge / dedup-merge / sweep / expire / polymorphize / compose / infect / atomize）

## 交付物

- schemas/upgrade/upgrade-proposal.schema.json
- packages/core/src/upgrade/propose.ts
- packages/cli/commands/upgrade-propose.ts
- fixtures/upgrade/proposal-pass.json
- fixtures/upgrade/proposal-blocked.json

## VALIDATION_CMD

```bash
npm test; npm run typecheck; npm run lint
```

## ROLLBACK_HINT

```bash
回退本卡 touched files；保持 schema-additive 與 adapter / core 邊界。
```

## 執行步驟

1. 在上游 repo 建立或修改對應 package / schema
2. 補 fixtures（含 positive / negative 與舊版相容）
3. 跑 package 驗證
4. 回寫 3KLife tracking notes 與 thin index

*由 GitHubCopilot 透過 ATM 演進規劃書 v2 開立 | 2026-05-06*

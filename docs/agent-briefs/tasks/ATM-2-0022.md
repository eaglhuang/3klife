---
doc_id: doc_task_0325
id: ATM-2-0022
priority: P0
phase: ATM-2
created: 2026-05-06
created_by_agent: GitHubCopilot
owner: GitHubCopilot
status: open
type: registry
chain_id: ATM-EVOLUTION-V2
chain_step: 9/11
sensor_triggered_by: atm-evolution-plan-v2
depends:
  - ATM-2-0014
  - ATM-2-0021
notes: |
  2026-05-06 | 狀態: open | 驗證: pending | 變更: 由 ATM 演進規劃書 v2 附錄 A 開立 | 阻塞: 等依賴卡完成
  2026-05-07 識別/行為/狀態機提案：追加 acceptance（schema-additive，不重啟驗收）— rollback proof 必須含 `behaviorId` 與對稱 rollback contract
---
# [ATM-2-0022] Rollback Registry Pointer & 三段 hash Proof

> **ATM 演進規劃 v2 開卡** — evolution 管線的最終安全網：失敗時可確定性回退，並留下可驗證 proof。
> **定位**：ATM-2
> **前置依賴**：ATM-2-0014 / ATM-2-0021

## 問題描述

提供 atm rollback CLI 與 rollback-proof：將 currentVersion 指回歷史版本，並驗證 spec/code/test 三段 hash 與目標 entry 完全一致。

## INPUT_CONTRACT

- 依賴卡片已 done 或可作 stub
- ATM 演進規劃書 v2 附錄 A 已併入規劃

## OUTPUT_CONTRACT (acceptance)

1. CLI atm rollback --atom <id> --to <ver> --plan 先輸出 dry-run plan；--apply 才真正寫 registry
2. rollback 後 atm verify --self 必須顯示 spec/code/test 三段 hash 與目標版本完全一致；任一不符即 hard fail，產出 rollback-proof.failure.json
3. rollback-proof.schema.json 含 atomId / fromVersion / toVersion / hashesVerified / verifiedAt / proofId
4. evidence store 寫入 evidenceType=rollback-proof

### 2026-05-06 附錄 B 原子地圖補丁（追加 acceptance）

5. CLI atm rollback 支援 `--target map`；map rollback 必須同時驗證所有成員 atom 的 hash 與目標 mapVersion entry 一致
6. rollback-proof 對 map 模式輸出 `memberAtomProofs[]`，逐一列三段 hash 驗證結果

### 2026-05-07 識別/行為提案追加（不重啟驗收）

7. rollback-proof.schema.json 必須含發起的 `behaviorId`（哪個 behavior 的 rollback；例如 `behavior.evolve` / `behavior.merge`）
8. rollback contract 對稱性：每個 behavior 的 forward action 必須有對應的 reverse action；rollback 引擎可從 behaviorId 查 reverse 規則

## 交付物

- packages/core/src/registry/rollback.ts
- packages/cli/commands/rollback.ts
- schemas/registry/rollback-proof.schema.json
- fixtures/rollback/proof-pass.json
- fixtures/rollback/proof-fail-hash-mismatch.json

## VALIDATION_CMD

```bash
npm test; npm run typecheck; npm run lint
```

## ROLLBACK_HINT

```bash
回退本卡 touched files；保持 schema-additive 與 adapter / core 邊界。
```

## 執行步驟

1. 在上游 repo 建立或修改對應 package / schema
2. 補 fixtures（含 positive / negative 與舊版相容）
3. 跑 package 驗證
4. 回寫 3KLife tracking notes 與 thin index

*由 GitHubCopilot 透過 ATM 演進規劃書 v2 開立 | 2026-05-06*

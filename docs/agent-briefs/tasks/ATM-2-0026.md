---
doc_id: doc_task_0332
id: ATM-2-0026
priority: P0
phase: ATM-2
created: 2026-05-07
created_by_agent: ClaudeCode_opus-4-7
owner: ClaudeCode_opus-4-7
status: open
started_at: "2026-05-07T00:00:00+08:00"
started_by_agent: ClaudeCode_opus-4-7
type: registry
chain_id: ATM-IDENTITY-BEHAVIOR-V1
chain_step: 1/8
sensor_triggered_by: atm-evolution-plan-identity-behavior
depends:
  - ATM-2-0014
  - ATM-2-0023
notes: |
  2026-05-07 | 狀態: open | 驗證: pending | 變更: 由 ATM 識別/行為/狀態機提案開立（chain ATM-IDENTITY-BEHAVIOR-V1 1/8 anchor）；schema-additive only、hot/cold 分離、性能上限 ≤10 hot 欄位 | 阻塞: 等 ATM-2-0014 / ATM-2-0023 完成
  2026-05-07 chain layout 已落地：8 張新卡（0026~0033）+ 12 張既有卡 acceptance 追加 + 文件 Part V/D12/開發計畫書同步；schema 實作仍 pending
  2026-05-07 備註修正批次：+deployScope/mutabilityPolicy 欄位 +version-index 三層比對 +行為參考手冊+SVG 狀態機圖
  2026-05-07 ATM-2-0042 連鎖影響：追加 AC（generator SF alignment + dogfood map 回歸驗證）
  2026-05-07 附錄 C 治理補丁：semanticFingerprint 改為 per-version 規則，hot entry 僅保留 currentVersion 指標
  2026-05-07 測試穩定性補丁：維持 schema extension 目的不變；補 SF validator 與 exemplar quartet，讓 hot/cold / per-version 破口先在本卡 seam 定位
---
# [ATM-2-0026] Semantic Fingerprint + Lineage + TTL Schema Extension

> **ATM 識別/行為/狀態機提案開卡** — 為原子與原子地圖加入 semanticFingerprint / lineage / ttl，並維持 hot/cold 分離以支援大規模原子庫。
> **定位**：ATM-2
> **前置依賴**：ATM-2-0014 / ATM-2-0023

## 問題描述

為 atom 與 atomic-map 加入 semanticFingerprint / lineage / ttl optional 欄位（schema-additive）；採 hot/cold 分離設計：hot registry entry 只放 sf 字串與 lineageLogRef / evidenceIndexRef URI，cold artifact 放完整 history。為去重警察與生命週期警察提供識別基礎。

## INPUT_CONTRACT

- ATM-2-0014 registry version history 已落地
- ATM-2-0023 atomic-map schema 已落地

## OUTPUT_CONTRACT

- [ ] atomic-spec / atomic-map schema additive 擴充
- [ ] sf 計算合約
- [ ] hot/cold registry layout policy
- [ ] spec schema 新增 `deployScope` / `mutabilityPolicy` 欄位（nested ≤ 2 層）
- [ ] 提供 version-index schema + 跨系統三層比對演算法規格（atomId → version → sf）
- [ ] `generateAtomicMap()`（ATM-2-0042）必須呼叫本卡 SF calculator 而非自行計算；若本卡未完成則 generator 設 SF = null + `pendingSfCalculation: true`
- [ ] Map SF 合約 `sortByName(entrypoints)+qualityTargets` 須用 ATM-2-0042 dogfood map 做回歸驗證
- [ ] semanticFingerprint 必須採 **per-version** 規則：hot registry entry 只保留 `currentVersion` 的 `semanticFingerprint`，歷史版本值寫入 `versions[*].semanticFingerprint`
- [ ] `node scripts/validate-semantic-fingerprint.mjs` 必須可獨立驗證 atom / map / legacy-no-sf / `pendingSfCalculation` 四組 exemplar，並檢查 hot/cold 與 per-version 規則
- [ ] exemplar 至少保留 `atom-with-sf` / `map-with-sf` / `legacy-no-sf` / `pending-sf-calculation` 四組，供 validator 直接重放

## 交付物

- schemas/atomic-spec.schema.json（additive）
- schemas/registry/atomic-map.schema.json（additive）
- packages/core/src/registry/semantic-fingerprint.ts
- scripts/validate-semantic-fingerprint.mjs
- fixtures/registry/atom-with-sf.json
- fixtures/registry/map-with-sf.json
- fixtures/registry/legacy-no-sf.json
- fixtures/registry/pending-sf-calculation.json

## VALIDATION_CMD

```bash
node scripts/validate-semantic-fingerprint.mjs
npm test; npm run typecheck; npm run lint
```

## ROLLBACK_HINT

```bash
回退本卡 touched files；schema 須維持 additive，不得改既有欄位語意。
```

## 執行步驟

1. 先定義 SF canonicalization 與 exemplar quartet（atom / map / legacy / pending）
2. 為 atomic-spec.schema.json 加 sf/lineage/ttl optional 欄位
3. 為 atomic-map.schema.json 加同類欄位
4. 實作 sf 計算與 hot/cold 切分
5. 先跑局部 validator，再補 fixtures 與舊版相容測試

*由 GitHubCopilot 透過 task-card-opener 開立 | 2026-05-07*

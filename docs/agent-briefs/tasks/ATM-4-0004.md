---
doc_id: doc_task_0271
id: ATM-4-0004
priority: P1
phase: ATM-4
created: 2026-05-05
created_by_agent: GitHubCopilot
owner: GitHubCopilot
status: open
type: atom-case
depends:
  - ATM-4-0003
notes: "2026-05-05 | 狀態: open | 驗證: pending | 變更: task-card-opener 產生 HARN rich brief | 阻塞: none"
---
# [ATM-4-0004] parseCssLength 與 parseFragmentList case atoms

> **ATM adapter/case/release 開卡** — 延伸 H2U case study，再抽 parseCssLength 與 parseFragmentList 兩個低風險純 atom。
> **定位**：ATM-4
> **前置依賴**：`ATM-4-0003`

## 問題描述

延伸 H2U case study，再抽 parseCssLength 與 parseFragmentList 兩個低風險純 atom。本卡承接 `ATM-4-0003` 已完成的 normalizeCssColor atomize lineage 規則；新 case atom 必須走 `behavior.atomize`，保留 legacy parentRefs 與 lineage log，不另開重複的 H2U atomize 卡。

## INPUT_CONTRACT

- `ATM-4-0003` 已提供 normalizeCssColor case atom、legacy fixture、lineage log 與 registry/hash entry。
- `ATM-2-0033` 已定義 ProjectAdapter `legacy://` URI 與 atomize / infect adapter contract。
- 本卡只能新增 parseCssLength / parseFragmentList case atoms，不得重寫 normalizeCssColor done card。

## OUTPUT_CONTRACT

- [ ] parseCssLength atom
- [ ] parseFragmentList atom
- [ ] fixtures
- [ ] registry/hash entries
- [ ] 兩個新 atom 均使用 `behavior.atomize`，並包含 `lineage.bornBy=atomize`。
- [ ] 兩個新 atom 均帶 `parentRefs=["legacy://3KLife/<path>#L<start>-<end>"]` 或等價 legacy URI，且 lineage log 可追溯 source range。
- [ ] neutrality scan 不得讓 3KLife / Cocos / html-to-ucuf 私有假設流入 upstream core schema。

## 交付物

- H2U parseCssLength atomic spec。
- H2U parseFragmentList atomic spec。
- positive / negative / legacy fixtures。
- registry/hash entries。
- lineage logs and parentRefs for both extracted atoms。
- validation report。

## VALIDATION_CMD

```bash
npm.cmd run check:encoding:touched -- --files docs/agent-briefs/tasks/ATM-4-0004.md
node tools_node/compute-gate.js --profile standard --agent-feedback
```

## ROLLBACK_HINT

```bash
回退本卡 touched files；adapter 變更不得回寫 upstream core，需另開 upstream PR。
```

## 執行步驟

1. 確認 upstream/downstream 邊界
2. 讀取 legacy source range，為 parseCssLength / parseFragmentList 建立 `legacy://` parentRefs。
3. 以 `behavior.atomize` 產生兩個 case atom spec、fixtures、registry/hash entry 與 lineage log。
4. 跑 adapter/case validation 與 neutrality scan。
5. 回寫 tracking notes。

*由 GitHubCopilot 透過 task-card-opener 開立 | 2026-05-05*

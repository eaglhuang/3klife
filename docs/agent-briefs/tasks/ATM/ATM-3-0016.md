---
doc_id: doc_task_0366
id: ATM-3-0016
priority: P1
phase: ATM-3
created: 2026-05-10
created_by_agent: codex-gpt-5
owner: codex-gpt-5
status: open
type: implementation
depends:
  - ATM-2-0029
  - ATM-2-0031
  - ATM-2-0033
notes: |
  2026-05-10 | 狀態: open | 驗證: pending | 變更: 新開 M3 缺口承接卡（validator orchestrator / AJV cache unified entry）| 阻塞: none
---
# [ATM-3-0016] Validator Orchestrator 與 AJV Cache 統一入口

> **ATM M3 gap closure**：補齊里程碑 M3 尚未落地的 validator orchestrator / AJV cache 統一入口。  
> **所屬階段**：ATM-3 / M3 機器驗證層  
> **前置依賴**：`ATM-2-0029`、`ATM-2-0031`、`ATM-2-0033`

## 目標與範圍

建立單一 validator orchestrator 入口與共享 AJV cache lifecycle，避免多條驗證鏈各自 compile schema、造成重複成本與驗證漂移。

## INPUT_CONTRACT

- 既有 `validate:*` 驗證腳本與 ATM-2 behavior / semantic validator surfaces

## OUTPUT_CONTRACT

- [ ] 單一 validator orchestrator API（run / registry / cache / telemetry）
- [ ] AJV cache registry 與 deterministic invalidate 規則
- [ ] 至少 3 條既有 `validate:*` 腳本改走統一入口
- [ ] compile metrics 與 cache hit/miss 報告
- [ ] M3 契約文件（orchestrator + AJV cache）

## VALIDATION_CMD

```bash
npm.cmd run check:encoding:touched -- --files docs/agent-briefs/tasks/ATM/ATM-3-0016.md
node tools_node/check-doc-shard-health.js
node tools_node/sync-atm-stabilization-milestone.js --check --strict
```

## ROLLBACK_HINT

```bash
回退 orchestrator/cache 相關 touched files，恢復 validate:* 原本入口並重跑 sync check。
```

## 執行步驟

1. 盤點現有 `validate:*` 入口與重複 AJV compile 熱點。
2. 建立 orchestrator + cache registry。
3. 遷移至少 3 條驗證鏈至統一入口並保留舊行為對照。
4. 補齊文件與 task-store / milestone 同步證據。

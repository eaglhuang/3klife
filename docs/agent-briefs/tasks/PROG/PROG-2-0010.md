---
doc_id: doc_task_0230
id: "PROG-2-0010"
priority: "P0"
owner: "vs-code-gpt-5.3-codex"
status: "in-progress"
type: "regression-baseline"
phase: "Plan5"
created: "2026-05-05"
created_by_agent: "ClaudeCode_claude-sonnet-4-6"
started_at: "2026-05-12T21:46:49.5319874+08:00"
started_by_agent: "vs-code-gpt-5.3-codex"
related_cards:
  - "PROG-2-0007"
  - "PROG-2-0005"
  - "PROG-2-0009"
depends:
  - "PROG-2-0005"
  - "PROG-2-0009"
notes: "2026-05-13 | 狀態: in-progress | 變更: 已完成 3 組 formal capture + workflow（gacha-ds3 / lobby-main-screen / battle-hud-screen），3 份 workflow-summary 皆 debugOnly=false，並產出 artifacts/html-to-ucuf-plan5/regression-summary.json 與 fixtures/html-to-ucuf-plan5 三組 input/source-package | 阻塞: 三組 adjustedScore 最高僅 0.64749，未達 acceptance 的 >=0.95（需後續 renderer parity / assetization 修復）"
---

# [PROG-2-0010] gacha-ds3 formal rerun + 2 個非 gacha fixture（多畫面 baseline）

## 開單原因

postmortem §根本原因指出：**Plan1-4 都在 gacha-ds3（或前身 GachaMain）上反覆迭代，規則容易 over-fit。Plan3 修「右側 tab-rail」對 gacha 有效但對其他畫面無意義。Plan5 PROG-2-0007 才要求 3 個 fixture，但目前只有 1 個。**

沒有 multi-fixture matrix，「規則的通用性」無法驗證；每次以為快達成 95% 時，發現只對單張畫面成立。本卡先完成「跑出 3 個 baseline 數據」，作為 PROG-2-0007 95% closure 的前置。

## INPUT_CONTRACT

- PROG-2-0005 的 runtime renderer parity 改善已 in-progress
- PROG-2-0009 的 final diff owner mapper Slice #4（capture/geometry protocol gate）已完成
- `tools_node/run-html-to-ucuf-workflow.js` formal capture protocol 可用
- 候選非 gacha source package（建議：button-family、character-detail，或同等代表性畫面）已存在於 art 來源

## OUTPUT_CONTRACT

- [ ] 在 `fixtures/html-to-ucuf-plan5/` 下建立 2 個非 gacha fixture（input.html + 對應 source package）
- [ ] 以 formal capture protocol 重跑 gacha-ds3（debugOnly=false，full-size capture）
- [ ] 3 個 fixture 各自跑完 workflow 並產出 `workflow-summary.json`
- [ ] 產出 `artifacts/html-to-ucuf-plan5/regression-summary.json`，內含 3 個畫面的：
  - `browserCoverage.adjustedCoverage`
  - `cocosFinalGate.adjustedScore`
  - `zoneOwnership.compactSummary`（top 20 buckets 按 owner 分類）
  - `nextFixes`（若有任何 score < 0.95）
- [ ] 至少 1 個 fixture 達 adjustedScore ≥ 0.95；未達者 nextFixes 必指向具體 source selector / UCUF node / runtime owner

## VALIDATION_CMD

```bash
# 1. fixture 檔案存在
ls fixtures/html-to-ucuf-plan5/
# 期望: gacha-ds3 + 2 個非 gacha

# 2. 三個 workflow summary
ls artifacts/html-to-ucuf-plan5/*/workflow-summary.json
# 期望: 3 個檔案，每個 debugOnly=false

# 3. regression-summary.json 完整
node -e "
const s=require('./artifacts/html-to-ucuf-plan5/regression-summary.json');
console.log('fixtures:', Object.keys(s.fixtures).length);
console.log('all have adjustedScore:', Object.values(s.fixtures).every(f=>typeof f.cocosFinalGate.adjustedScore==='number'));
console.log('any reach 0.95:', Object.values(s.fixtures).some(f=>f.cocosFinalGate.adjustedScore>=0.95));
"

# 4. compute-gate
node tools_node/compute-gate.js --profile standard --agent-feedback
```

## ROLLBACK_HINT

```bash
rm -rf fixtures/html-to-ucuf-plan5/<new-fixture-1>/
rm -rf fixtures/html-to-ucuf-plan5/<new-fixture-2>/
rm artifacts/html-to-ucuf-plan5/regression-summary.json
```

## 建議作法

1. **挑選非 gacha fixture**：找 2 個結構性差異大的畫面（例如：純文字密集的 character-detail vs CTA-heavy 的 button-family）
2. **gacha-ds3 重跑**：用最新 `formal capture protocol`，確保 actualScreenId == expectedScreenId == gacha-ds3
3. **新 fixture 走完 source-dir → workflow → editor capture**：每個都產 workflow-summary.json
4. **regression-summary.json 結構**：
   ```json
   {
     "generatedAt": "2026-05-05T...",
     "fixtures": {
       "gacha-ds3": { "browserCoverage": {...}, "cocosFinalGate": {...}, "zoneOwnership": {...}, "nextFixes": [...] },
       "<fixture-2>": {...},
       "<fixture-3>": {...}
     },
     "summary": { "anyMet095": true/false, "highestScore": 0.XX, "lowestScore": 0.XX }
   }
   ```
5. **不要手改 *.json 救分**：所有改善要從 source 端流回（postmortem §B4 提醒）

## 交付物

- `fixtures/html-to-ucuf-plan5/<non-gacha-1>/`（新建：input.html + source package）
- `fixtures/html-to-ucuf-plan5/<non-gacha-2>/`（新建）
- `artifacts/html-to-ucuf-plan5/<gacha-ds3-fresh>/workflow-summary.json`（新跑）
- `artifacts/html-to-ucuf-plan5/<non-gacha-1>/workflow-summary.json`（新跑）
- `artifacts/html-to-ucuf-plan5/<non-gacha-2>/workflow-summary.json`（新跑）
- `artifacts/html-to-ucuf-plan5/regression-summary.json`（新建）

## 估時

3-4 hours

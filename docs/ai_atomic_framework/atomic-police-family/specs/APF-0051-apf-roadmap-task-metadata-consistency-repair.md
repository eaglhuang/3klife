<!-- doc_id: doc_other_0716 -->
# APF-0051 APF roadmap/task metadata consistency repair

## Purpose

M14 repairs APF roadmap/task metadata so implementers can distinguish design specs from runtime scanner cards.

## Required output

- Main plan contains M14 audit conclusion.
- tasks README contains Spec -> Runtime scanner mapping.
- APF-0003~0007 contain `runtime_successor_task`.
- APF-0021~0025 contain `design_source_task`.
- APF-0040~0050 contain full file-boundary and non-goal frontmatter.

## Non-goal

No upstream runtime change is required for APF-0051.

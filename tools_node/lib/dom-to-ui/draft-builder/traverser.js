'use strict';

const core = require('../draft-builder-core');

module.exports = {
  buildDraftFromHtml: core.buildDraftFromHtml,
  inferNodeType: core.inferNodeType,
  anchorToWidget: core.anchorToWidget,
};
